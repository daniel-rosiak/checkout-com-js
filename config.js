const defaultConfig = {
  liveUrl: 'https://api.checkout.com',
  sandboxUrl: 'https://api.sandbox.checkout.com',
  clientVersion: '2.0',
  connectTimeout: 60,
  readTimeout: 60
};

module.exports = defaultConfig;